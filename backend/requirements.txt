# === Backend framework ===
fastapi==0.110.0
uvicorn[standard]==0.23.1

# === Data processing ===
numpy>=1.26.4,<2.0
pandas>=2.0.3,<3.0

# === ML / NLP ===
torch>=2.1.0
transformers>=4.35.0,<5.0
scikit-learn>=1.3.0,<2.0

# === Async / HTTP ===
httpx>=0.24.0,<0.25

# === Pydantic settings ===
pydantic>=2.3.0,<3.0

# === Redis cache (опционально) ===
redis>=5.3.0,<6.0

# === Logging / utils ===
loguru>=0.7.0,<1.0

# === Development tools (не обязательны для production) ===
python-multipart>=0.0.6,<1.0
pytest>=8.0.0,<9.0
pytest-asyncio>=0.22.0,<1.0

# === Optional: CORS и GZip (FastAPI middleware) ===
starlette>=0.28.0,<1.0
